[package]
name = "staaf-janus"
version = "0.1.0"
authors = ["eater <=@eater.me>"]
edition = "2018"

[lib]
crate_type = ["cdylib"]

[dependencies]
janus-plugin = "0.12.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.58"